import {getEl} from './dom';
/*
 * @Author: zhangfanglan@ 
 * @Date: 2017-12-22 20:18:45
  * 批量设置行内样式
 * @param {string} el: 需要找的元素
 * @param {string} cssInLine: 行内样式
*/
let setCss = (el, cssInLine) => {
    if (typeof el == 'object') {
        for (let attr in cssInLine) {
            el.style[attr] = cssInLine[attr];
        }
    } else {
        let objDom = this.getDom(obj);
        for (let item of objDom) {
            item.style.display = cssInLine;
        }
    }

};

let hide = (el) => {
    let dom = this.getDom(el);
    for (let item of dom) {
        item.style.display = 'none';
    }
}

let show = (el) => {
    let dom = this.getDom(el);
    for (let item of dom) {
        item.style.display = 'block';
    }
}

export {
    setCss,
    show,
    hide
};