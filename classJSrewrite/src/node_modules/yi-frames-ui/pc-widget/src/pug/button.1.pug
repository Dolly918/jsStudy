mixin button(...items)
	each item in items
		//- a
		if (item.labelType == 'a')
			if(item.border=='border')
				a.yi-btn.bor(href="####" class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text
			else if(item.type=='bg-primary' || item.type=='bg-success' || item.type=='bg-danger' || item.type=='bg-stress' || item.type=='bg-warning' || item.type=='bg-hollow')
				button.yi-btn(class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text
			else
				button.yi-btn.bor(class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text

		//- div
		else if (item.labelType=='div')
			if(item.labelType=='div')
				.yi-btn.bor(class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text
			//- else if(item.type=='bg-primary' || item.type=='bg-success' || item.type=='bg-danger' || item.type=='bg-stress' || item.type=='bg-warning' || item.type=='bg-hollow')
			//- 	button.yi-btn(class!=item.type ,
			//- 		class!=item.size,
			//- 		disabled!=item.disabled,
			//- 		class!=item.disabled,
			//- 		class!=item.round,
			//- 		class!=item.load,
			//- 	)&attributes(attributes)!= item.text
			//- else
			//- 	button.yi-btn.bor(class!=item.type ,
			//- 		class!=item.size,
			//- 		disabled!=item.disabled,
			//- 		class!=item.disabled,
			//- 		class!=item.round,
			//- 		class!=item.load,
			//- 	)&attributes(attributes)!= item.text
		
		//- span
		else if (item.labelType == 'span')
			if(item.border=='border')
				a.yi-btn.bor(href="####" class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text
			else if(item.type=='bg-primary' || item.type=='bg-success' || item.type=='bg-danger' || item.type=='bg-stress' || item.type=='bg-warning' || item.type=='bg-hollow')
				button.yi-btn(class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text
			else
				button.yi-btn.bor(class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text
		
		//- button
		else
			if(item.border=='border')
				button.yi-btn.bor(class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text
			else if(item.type=='bg-primary' || item.type=='bg-success' || item.type=='bg-danger' || item.type=='bg-stress' || item.type=='bg-warning' || item.type=='bg-hollow')
				button.yi-btn(class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text
			else
				button.yi-btn.bor(class!=item.type ,
					class!=item.size,
					disabled!=item.disabled,
					class!=item.disabled,
					class!=item.round,
					class!=item.load,
				)&attributes(attributes)!= item.text

