mixin accordion(...items)
	//- .yi-accordion
	each item in items
		.accord-item
			.ai-title
				i.icon(class!= iconClass)!= item.icon
				| #{item.title}
			
			if(item.isShow=='show')
				.ai-content.block!= item.content
					block
			else
				.ai-content.hide!= item.content
					block
