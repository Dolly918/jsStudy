mixin modal(modals)
	.yi-modal&attributes(attributes)
		if modals.close
			.ml-close.close!= modals.close
		if modals.title
			.ml-hd!= modals.title
		if modals.text
			.ml-bd
				.text!= modals.text
		.ml-ft
			if modals.btnOk
				button.btnok(type="button" class!=modals.btnOkClass)!= modals.btnOk
			if modals.btnNo
				button.btnno(type="button" class!=modals.btnNoClass)!= modals.btnNo