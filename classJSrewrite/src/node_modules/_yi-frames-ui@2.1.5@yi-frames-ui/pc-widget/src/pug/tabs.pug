mixin tabs(...items)
	.yi-tabs&attributes(attributes)
		ul.tab-nav.tabs-ul
			each item, index in items
				li(class!= item.active)!= item.text
					//- a.link(href="#") #{item}
		//.tab-panel
		if block
			block