mixin toast(...items)
	each item in items
		.yi-toast(
			class!= item.widthAuto
			class!= item.class
		)&attributes(attributes)
			if item.iconClass
				i.icon(
					class!= item.iconBlock
					class!= item.iconClass
				)
			if item.text
				.text!=item.text