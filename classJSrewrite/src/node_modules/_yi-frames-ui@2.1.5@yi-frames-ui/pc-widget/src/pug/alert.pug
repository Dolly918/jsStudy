mixin alert(...items)
	.yi-alert&attributes(attributes)
		each item in items
			.item(class!= item.type)
				if(item.closeType == 'icon')
					i.close ×
				else
					span.close!= item.closeType
				.text!= item.text
